use std
use date
use "dispatch"

const date = {params
	std.put("date called\n")
	//std.put("{}\n",date.utcnow())
	std.put("got {}\n", date.utcnow())
}

const __init__ = {
	dispatch.register("date", date)
}
