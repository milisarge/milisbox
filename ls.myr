use std
use "dispatch"


const ls = {params
	var dir, output
	std.put("ls called\n")
	dir=params[0]
	output=std.byentry(dir)
    std.put("{}\n",output.nextfile)
}

const __init__ = {
	dispatch.register("ls",ls)
}
