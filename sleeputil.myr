use std
use "dispatch"


const sleeputil = {params
	var sn :time
	std.put("sleep called\n")
	std.put("s.\n")
	sn=params[0]
	//std.usleep(4000000) //4sec
	std.usleep(sn) //4sec
    std.put("f.\n")
}

const __init__ = {
	dispatch.register("sleep",sleeputil)
}
